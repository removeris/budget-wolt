package com.example.budgetwolt.models;

public class Cuisine {
    private int id;
    private enum ingredients{};
    private enum allergens{};
    private enum portionSize{};
    private double price;
}
